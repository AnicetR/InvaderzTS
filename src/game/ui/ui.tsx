import * as React from 'react';
import './ui.scss';
import { DebugContainer } from './components/debug/debugContainer';

function UI() {
  return (
    <div className="UI">
      <DebugContainer/>
    </div>
  );
}

export default UI;